1. 文件说明
（1）src.cpp为程序源码文件；
（2）bin目录为二进制文件夹：
     3DModelManipulator.exe为程序文件；
     bdf2.ply2为模型文件；
     bdf2.rr为谷脊点文件；
     其余为程序依赖文件。

2. 使用说明
（1）双击3DModelManipulator.exe即可打开程序；
（2）按F键退出全屏，OSG窗口为操作窗口，CMD窗口为状态显示窗口；
（3）在操作窗口鼠标左键点击，选择要操作的点；
（4）程序会找到距离点击点最近的谷脊点，并生成拖拽器；
（5）用鼠标通过拖拽器进行交互式操作；
（6）拖拽器移动到合适位置时，按R键获得移动向量，并更新模型显示；
（7）上述步骤（3-6）可重复进行；
（8）关闭操作窗口，程序将自动保存模型为OBJ和OSG文件；
（9）保存文件完毕后，关闭CMD窗口即可。

3. 进度
（1）程序代码实现问题基本解决，还没有迁移到MFC平台；
（2）变形的核心算法和参数还需要实验确定。
